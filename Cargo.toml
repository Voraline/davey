[workspace]
resolver = "2"

members = [
    "davey",
    "davey-node",
    "davey-python",
]

[profile.release]
opt-level = 3
lto = "fat"
codegen-units = 1
panic = "abort"
overflow-checks = false
debug = false
strip = "symbols"
incremental = false
